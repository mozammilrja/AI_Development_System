# Database Migrations

This directory contains database migration scripts managed by Alembic (or a
TypeScript migration tool in future iterations).

## Directory Structure

```
migrations/     — Migration version files
alembic.ini     — Alembic configuration (legacy)
```

## Owner

**Backend Agent** — database migrations are the Backend agent's responsibility.
